<div class="page-content">
<div class="entry-content -cf">
<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> No se puede guardar el registro:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nombre" %><br>
    <%= f.text_field :name %>
  </div><br>
  <div class="field">
    <%= f.label :email, "Email" %><br>
    <%= f.text_field :email %>
  </div><br>
  <div class="field">
    <%= f.label :cellphone, "Celular" %><br>
    <%= f.text_field :cellphone %>
  </div><br>
  <div class="field">
    <%= f.label :commentaries, "Comentarios" %><br>
    <%= f.text_area :commentaries %>
  </div><br>
  <div class="actions">
    <%= f.submit "Enviar", :class => "link-button green" %>
  </div><br>
<% end %>
</div>
</div>
