<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Germaine de Capuccini" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <meta charset="utf-8">
    <title>Germaine de Capuccini</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width">
    <link href="http://fonts.googleapis.com/css?family=Arvo:400" rel="stylesheet" type="text/css">
    <link href="http://fonts.googleapis.com/css?family=Lato:400,700" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/responsive.css">
    
    <script src="js/modernizr.js"></script>
    <script src="http://api.twitter.com/1/statuses/user_timeline.json?include_entities=true&amp;count=100&amp;include_rts=true&amp;exclude_replies=true&amp;screen_name=ansimuz&amp;callback=Chirp.callback1"></script>
  </head>
  <body>
    <div class="wrapper">
      <%= render 'layouts/header' %>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <div id="main" class="cf">
        <div class="page-content">
          <div class="entry-content -cf">
            <%= yield %>
          </div>
        </div>
      </div>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
